### LOG Levels: 3=DBG, 2=INFO, 1=NOTICE, 0=WARN, -1=ERR
debug=2
log_stderror=no

memdbg=5
memlog=5

log_facility=LOG_LOCAL0
log_prefix="{$mt $hdr(CSeq) $ci} "

/* number of SIP routing processes */
children=2

/* uncomment the next line to disable TCP (default on) */
disable_tcp=yes

/* uncomment the next line to disable the auto discovery of local aliases
 * based on reverse DNS on IPs (default on) */
# auto_aliases=no

/* add local domain aliases */
# alias="sip.mydomain.com"

/* listen addresses */
#!substdef "!PUBLICIP!$env(PUBLICIP)!"
#!substdef "!COREIP!$env(COREIP)!"
#!substdefs "!CORESUBNET!$env(CORESUBNET)!"

#!substdef "!UDPPORT!$env(UDPPORT)!"
#!substdef "!TLSPORT!$env(TLSPORT)!"

#!substdef "!PUPLIC_UPD_SOCKET!udp:PUBLICIP:UDPPORT!"
#!substdef "!PUPLIC_TLS_SOCKET!tls:PUBLICIP:TLSPORT!"

#!substdef "!CORE_UPD_SOCKET!udp:COREIP:UDPPORT!"
#!substdef "!CORE_TLS_SOCKET!tls:COREIP:TLSPORT!"

listen=PUPLIC_UPD_SOCKET
###listen=PUPLIC_TLS_SOCKET
listen=CORE_UPD_SOCKET
###listen=CORE_TLS_SOCKET