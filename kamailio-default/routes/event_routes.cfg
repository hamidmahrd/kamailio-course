event_route[topos:msg-outgoing] {
    xlog("L_INFO", "topos:msg-outgoing | \n");
    if !(is_in_subnet($si, 'CORESUBNET')) {
        xlog("L_INFO", "topos:msg-outgong | request from Public->Core. Dont strip via Topos \n");
        drop;
    }
}